# Holoscan-Add-Matrices
Project consists of a cuda kernel used in a holoscan project. The application has two operators where the first operator memory and data and the second operator calls the CUDA kernel
